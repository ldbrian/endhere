### 📄 02_Architecture.md（技术与物理架构）

**I. 空间物理法则 (Space Physics)**
为了防止空间自由沦为沙盒混乱，所有物件交互必须由极简的“黑白名单字典”进行代码阻断：

* **Surface（水平承载面）：** 抵抗重力托举（如吧台面、破木凳）。严格遵循面积与排他性互斥（如凳子放了猫则人不能坐，人坐下后凳子不可移动）。交互方向仅改变物理姿态。


* **Container（内部容纳）：** 三维包裹与空间隔绝（如冰柜、铁筐）。遵循严格的体积与类型黑名单拦截（UI 层物理阻断活物或大型家具）。


* **Attachment（垂直附着 / 无极承载）：** 依附于其他实体表面（如墙面、冰柜门）。允许叠加、覆盖与视觉混乱感，承载时间斑驳。



**II. 技术实施规范 (ECS Lite 架构)**
摒弃复杂的物理碰撞引擎，采用状态与规则解耦的极简架构：

* **静态物品字典 (Static Entity Dictionary)：** 所有实体开发前必须在全局字典注册属性、黑白名单边界与排他性校验。


* **动态运行时状态 (Runtime State)：** 用户状态与物件状态独立维护，前端 UI 依靠判断坐标重叠来渲染交互，严禁硬编码绑定逻辑。


* **解耦“姿态”与“环境”：** 用户在任何位置触发交互功能完全一致，仅在文学描述（Flavor Text）上呈现颗粒度差异。



---

