《End Here》空间架构与底层法则 V1.0
一、 产品终极定位：数字空间设计实验
《End Here》不是一个传统的“情绪树洞产品”，也不是一个带有任务系统的“解谜挂机游戏”。
它是一个“数字空间设计实验”。

核心北极星指标：停留（Stay）。 倾诉、发呆、阅读，都只是“停留”时可能发生的副产品。我们提供的是现代互联网绝对不提供的奢侈品——“不被催促的、被允许浪费的时间”。

最高设计哲学：物件先于意义（存在先于本质）。 我们不设计“功能”（如：发呆区、宣泄区），我们只提供遵循物理规则的“物件”（如：凳子、冰柜）。意义由用户在规则内通过交互自行赋予。

二、 空间物理法则（世界观规则）
为了防止“自由”变成“混乱”（如把猫塞进冰箱），空间内的所有物件交互必须严格遵循以下三套物理维度的从属关系：

1. Surface（水平承载面）
物理特征： 抵抗重力的托举（如：吧台面、破木凳）。

规则限制： 严格的面积与排他性互斥。例如，凳子上如果放了“流浪猫”，用户就无法执行“坐下”；用户“坐下”后，凳子不可被移动。

交互方向： 改变用户的物理姿态（站立 ↔ 坐下）。

2. Container（内部容纳）
物理特征： 三维包裹，空间隔绝或状态改变（如：冰柜、抽屉、铁筐）。

规则限制： 极简的“黑白名单”拦截。遵循体积和类型限制，绝对黑名单物品（如活物、大型家具）在 UI 层直接物理阻断，不予渲染操作按钮。

3. Attachment（垂直附着/无极承载）
物理特征： 依附于其他实体的表面（如：墙上的海报、冰柜门的便利贴）。

规则限制： 极度自由，允许叠加、覆盖与视觉上的“混乱感”，体现时间的斑驳。

三、 交互与体验底线（Anti-Gamification）
所有前端 UI 与交互设计，必须死守以下纪律，剥离一切互联网产品的“规训感”：

物理动词替换功能名词： 永远不要在按钮上教用户做事。干掉 > 发呆，替换为 > 闭上眼睛 或 > 盯着墙根看。不总结用户的状态，只描述客观的动作。

解耦“姿态”与“环境”：
用户在空间的任何位置都可以触发交互，区别仅在于文学描述（Flavor Text）的不同。站着写便签和坐着写便签，功能完全一致，仅在 UI 文本反馈上呈现颗粒度差异。

禁止系统劫持（Auto-redirect）：
绝对不允许基于时间的“强制页面跳转”。时间的流逝只能通过“渐进式文本（Progressive Disclosure）”浮现，始终把控制权（钥匙）留在用户手里。

环境底噪引擎（Ambient Engine）：
废弃线性的固定剧本。采用随机低概率的环境文本池（如：[ 风扇还在转 ]、[ 门外似乎有人经过 ]），通过极慢的 CSS 渐隐渐现，让空间产生真实的“呼吸感”。

四、 技术实施规范（ECS Lite 架构）
为了保证单人开发者的可维护性，摒弃复杂的物理碰撞引擎，采用状态与规则解耦的极简架构：

1. 静态物品字典 (Static Entity Dictionary)
所有实体进入开发前，必须在全局字典注册属性与黑白名单边界。

JavaScript
// Entity Dictionary 范例
export const SPACE_RULES = {
  stool: {
    type: 'surface',
    movable: true,
    allowed_locations: ['corner', 'bar'],
    allowed_on_top: ['human', 'cat'], // 白名单
    is_exclusive: true // 排他性校验
  },
  fridge: {
    type: 'container',
    movable: false,
    allowed_locations: ['bar'],
    blocked_entities: ['cat', 'stool', 'human'] // 黑名单直拦
  }
}
2. 动态运行时状态 (Runtime State)
用户的状态和物件的状态独立维护，前端 UI 通过判断两者坐标的重叠来渲染交互。

JavaScript
// 状态解耦模型
const userState = {
  location: 'bar', // 位置坐标
  posture: 'sitting' // 物理姿态
}

const stoolState = {
  location: 'bar', // 位置坐标
  on_top: 'human' // 承载状态
}
开发纪律：严禁在 Location 组件内部硬编码绑定 Stool 的逻辑，一切依靠全局状态机驱动文本条件渲染。

五、 品牌宣发纪律（CMO 手册）
《End Here》的对外发声，必须维持“极其粗糙的真实感”与“高度的疏离感”。

废弃“更新公告”，改为“店长随笔”： 记录状态，不推销功能。

绝对克制： 不预告惊喜，不索取点赞，不解答隐秘交互（如 10 秒静默彩蛋由用户自行探索）。

视觉写实： 永远不用 AI 精美绘图或包装过的效果图。使用能证明“在物理废墟里建赛博避难所”的真实随手拍（如地下车库、屏幕摩尔纹、纯文本代码切片）。

人设基调： 疲惫的网约车司机、克制的赛博空间主理人。“我修了代码。没什么特别的用处。门没锁，随便坐。”